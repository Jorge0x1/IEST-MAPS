{
  "name": "iestmaps-workspace",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm install && npm --prefix iestmaps_api install && npm --prefix iestmaps_react install",
    "db:setup": "node scripts/db-setup.js",
    "predev": "npm run db:setup",
    "predev:api": "npm run db:setup",
    "predev:no-tunnel": "npm run db:setup",
    "dev": "node scripts/dev-with-tunnel.js",
    "dev:api": "npm --prefix iestmaps_api run dev",
    "dev:web": "npm --prefix iestmaps_react run dev",
    "dev:no-tunnel": "concurrently -n API,WEB -c green,cyan \"set PORT=3001&& set FRONTEND_URL=http://localhost:5174&& npm --prefix iestmaps_api run dev\" \"npm --prefix iestmaps_react run dev -- --port 5174 --strictPort\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv": "^16.6.1",
    "mysql2": "^3.14.5"
  }
}
